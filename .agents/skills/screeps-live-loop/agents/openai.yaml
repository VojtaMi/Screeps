interface:
  display_name: "Screeps Live Loop"
  short_description: "Edit, deploy, and verify the Screeps bot against live game state"
  default_prompt: "Use the Screeps live loop to make a bot change, verify locally, push/deploy through GitHub Actions when requested, and inspect live game state afterward."
