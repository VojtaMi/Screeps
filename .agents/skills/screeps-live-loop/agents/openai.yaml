interface:
  display_name: "Screeps Live Loop"
  short_description: "Goal-driven Screeps edit, deploy, and live verify"
  default_prompt: "Use $screeps-live-loop to make a Screeps bot change, verify locally, publish only when explicitly requested, and confirm the result in live game state."
