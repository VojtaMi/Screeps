export const CREEP_BODY = {
  PIONEER: [WORK, CARRY, MOVE, CARRY, MOVE],
  HARVESTER: [WORK, CARRY, MOVE, WORK, WORK, MOVE, WORK, WORK, MOVE],
  CARRIER: [
    MOVE,
    CARRY,
    MOVE,
    CARRY,
    MOVE,
    CARRY,
    MOVE,
    CARRY,
    MOVE,
    CARRY,
    MOVE,
    CARRY,
    MOVE,
    CARRY,
    MOVE,
    CARRY,
  ],
  DEFENDER: [
    TOUGH,
    ATTACK,
    MOVE,
    MOVE,
    TOUGH,
    ATTACK,
    MOVE,
    MOVE,
    TOUGH,
    ATTACK,
    MOVE,
    MOVE,
  ],
  WORKER: [WORK, CARRY, MOVE, WORK, CARRY, MOVE, WORK, CARRY, MOVE],
} satisfies Record<string, BodyPartConstant[]>;
